spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/board?serverTimezone=UTC&characterEncoding=UTF-8
    username: root
    password: 1234
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
  mvc:
    servlet:
      load-on-startup: 1
  security:
    user:
      name: user
      password: f4792016-f8ca-45b5-9e2a-f6b8a869d64e

server:
  port: 8097


logging:
  level:
    org:
      springframework:
        security: DEBUG